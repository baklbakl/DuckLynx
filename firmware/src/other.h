#ifndef OTHER_H
#define OTHER_H

//the other module is probably a bad idea, but it is where unclassified stuff goes for now. Maybe this stuff should be in sysctl

#include <stdint.h>
void failsafe(void);
void delay(uint32_t ticks);

#endif
